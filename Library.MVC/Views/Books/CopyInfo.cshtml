@model Library.MVC.Models.CopyInfoViewModel

@{
    ViewData["Title"] = "Copy Info";
}

<h1>Copy Info</h1>

<p>
    <a asp-action="CreateCopy" asp-route-id="@Model.BookDetailsId">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>ISBN</th>
            <th>Title</th>
            <th>Author</th>
            <th>Condition</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Copies)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details.ISBN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details.Author.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Condition)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookStatus)
            </td>
            <td>
                <a asp-action="DeleteCopy" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>


